# TODO

- [x] Update `app.py` to use an absolute SQLite DB path (robust for Docker/Jenkins).
- [x] Ensure DB initialization and all queries use the same `DB_PATH`.
- [x] Run the Flask app and confirm `/` loads and `/add` inserts rows.
- [x] Confirm error documentation remains in `ERROR_REPORT.md`.


